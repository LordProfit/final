import { Aldrich, Microgramma } from 'next/font/google'
 
export const aldrich = Aldrich({
  subsets: ['latin'],
  display: 'swap',
})
 
export const micro = Microgramma({
  subsets: ['latin'],
  display: 'swap',
})